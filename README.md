# Basic-logic-gates
Basic logic gates are digital circuits that perform logical operations on one or more binary inputs. The basic gates are AND, OR, and NOT, which are used to build complex digital circuits.
